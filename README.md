# get-rewards-multicall-txn

Get rewards for a multicall lootbox open txn.

### How to use?

- Install node modules: `npm i`
- Replace with your transaction hash here `txnHash` in [index.js](./index.js#L14-L15)
- Run `node index.js`
